{
  "name": "functions",
  "description": "Firebase Functions for GPT integration",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint ."
  },
  "main": "lib/index.js",
  "engines": {
    "node": "18"
  },
  "type": "module",
  "dependencies": {
    "openai": "^5.3.3",
    "firebase-admin": "^11.10.1",
    "firebase-functions": "^4.4.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}