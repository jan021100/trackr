import test from 'node:test'; import assert from 'node:assert/strict';
import { firstPassCompletionsByDay, groupSurgerySessionsByDay } from '../src/lib/study/surgeryChart.ts';
const session=(id,date,mastery,questions,assessed,topicIds=[])=>({id,date,label:id,questions,averageMastery:mastery,assessedTopics:assessed,createdAt:date,topicIds});
test('multiple patches on one day become one final daily point',()=>{const result=groupSurgerySessionsByDay([session('a','2026-08-14T08:00:00Z',2.2,4,25,['TO1-02']),session('b','2026-08-14T18:00:00Z',2.5,7,28,['TO1-08','TO1-09'])],'Europe/Prague');assert.equal(result.length,1);assert.equal(result[0].averageMastery,2.5);assert.equal(result[0].assessedTopics,28);assert.equal(result[0].questions,11);assert.equal(result[0].imports,2);assert.deepEqual(result[0].topicIds,['TO1-02','TO1-08','TO1-09'])});
test('calendar grouping uses the requested local timezone',()=>{const result=groupSurgerySessionsByDay([session('late','2026-08-14T23:30:00Z',2,1,1)],'Europe/Prague');assert.equal(result[0].date,'2026-08-15')});
test('daily series remains chronological when input is not',()=>{const result=groupSurgerySessionsByDay([session('b','2026-08-15T10:00:00Z',2.5,2,2),session('a','2026-08-14T10:00:00Z',2,1,1)],'Europe/Prague');assert.deepEqual(result.map(x=>x.date),['2026-08-14','2026-08-15'])});
test('first-pass bars count only explicit completion timestamps',()=>{const counts=firstPassCompletionsByDay({topics:{'TO1-01':{firstPassCompletedAt:'2026-08-26T09:00:00.000Z'},'TO1-02':{firstPassCompletedAt:'2026-08-26T11:00:00.000Z'},'TO1-03':{lastPlanReviewAt:'2026-08-26T12:00:00.000Z'},'TO1-04':{secondPassCompletedAt:'2026-08-26T13:00:00.000Z'}}});assert.deepEqual(counts,{'2026-08-26':2})});
